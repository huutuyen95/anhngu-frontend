import { StudentMissions } from "@/features/missions/student-missions";

/** Nhiệm vụ — mục tiêu em tự đặt từ Thư viện. Đây là màn chính sau khi đăng nhập. */
export default function MissionsPage() {
  return <StudentMissions />;
}
